# 【確定版 要件定義書】お気に入り銘柄 株価チャート自動配信システム

## 1. システム概要

本システムは、東証（東京証券取引所）上場銘柄の日足および週足ローソク足チャートを、ログインユーザーごとに登録されたお気に入り銘柄（最大30銘柄）について自動生成し、指定の日時に電子メールで配信するWEBアプリケーションおよびバックエンド自動処理システムである。

対象ユーザーは管理者によってあらかじめ登録された**最大5名**とし、**GitHub Pages**、**GitHub Actions**、**yfinance**、**Supabase (Free Tier)**等の無料サービスを組み合わせて**完全無料（0円）**で永続運用する。

---

## 2. システム構成・技術スタック

```mermaid
graph TD
    subgraph クライアント・WEBフロントエンド
        User[登録ユーザー (最大5名)] -->|スマホ / PC ブラウザ| GHP[GitHub Pages]
        GHP -->|認証 & お気に入りリスト(最大30件)| DB[(Supabase Free Tier)]
    end

    subgraph 自動配信バッチ (GitHub Actions)
        GHA[GitHub Actions Cron] -->|1. ユーザー＆お気に入り取得| DB
        GHA -->|2. 東証株価取得 yfinance| YF[Yahoo Finance API]
        GHA -->|3. チャート画像生成 mplfinance| Py[Python Execution Environment]
        Py -->|4. スマホ対応HTMLメール生成 & 送信| SMTP[Gmail SMTP / Resend]
        SMTP -->|5. 各ユーザーの指定メールアドレスへ送信| User
    end
```

| 区分 | 採用技術 / サービス | 設定・仕様 |
| :--- | :--- | :--- |
| **WEBフロントエンド** | HTML5 / CSS3 / Vanilla JS | モバイルファースト・レスポンシブデザイン。GitHub Pagesで無料配信 |
| **データベース / 認証** | Supabase (Free Tier) | 管理者事前発行アカウント（最大5名）・お気に入り銘柄（最大30件/人）を管理 |
| **データ取得** | Python `yfinance` | 東証銘柄コード（例: `7203.T`）の株価四本値・出来高をAPIキー不要で取得 |
| **チャート生成** | Python `mplfinance` / `matplotlib` | ローソク足、移動平均線、出来高をプロットした画像を出力 |
| **自動実行バッチ** | GitHub Actions (`schedule` cron) | 取引日17:00 JST (日足) / 土曜日 09:00 JST (週足) に自動起動 |
| **メール送信** | SMTP (Gmail) または Resend API | 登録ユーザー指定の電子メールアドレス宛にスマホ最適化HTMLメールを送信 |

---

## 3. 確定した機能仕様

### 3.1 WEBアプリケーション機能仕様

1. **ユーザー認証機能（管理者事前発行方式）**
   - 新規会員登録（セルフサインアップ）は無効化し、管理者がSupabaseダッシュボード上で発行したアカウント（メールアドレス＋パスワード）のみログイン可能。
   - セッション保持：ブラウザの `localStorage` にセッション情報を安全にトークン保存。

2. **お気に入り銘柄管理機能**
   - **銘柄コード入力バリデーション**：半角数字4桁（例: `7203`）のフォーマットチェックを実施。
   - **登録上限数の制御**：**1ユーザーあたり最大30銘柄**まで。フロントエンドおよびデータベース（SQL Trigger `enforce_favorites_limit`）の両重で上限超過登録をブロック。
   - **銘柄一覧表示・削除**：登録済み銘柄の一覧表示とワンタップ削除機能。

### 3.2 メール自動配信機能仕様

1. **スマホ最適化インラインHTMLメール**
   - **HTML構造**：`max-width: 100%` / レスポンシブ幅設定および `color-scheme: light` の明示により、iPhone/Androidの各種メールアプリ（ダークモード含む）で崩れず閲覧可能。
   - **インライン画像埋め込み**：各銘柄のローソク足チャート画像（PNG）をメール本文内にインライン埋め込み（`cid:` 参照）。
   - **銘柄サマリーカード**：銘柄コード、銘柄名、最新終値、前日比（騰落率）を各チャートの上部にカード型で明記。

2. **チャート描画仕様**
   - **日足チャート**：**直近 50営業日**のローソク足、移動平均線（5日/25日/75日）、出来高。
   - **週足チャート**：**直近 52週分（約1年）**のローソク足、移動平均線（13週/26週）、出来高。

3. **配信スケジュールおよび動作**
   - **日足配信**：東証取引日（月〜金）の **毎日17:00〜17:30 JST**（`jpholiday` および `yfinance` による休業日自動判定。非営業日は自動スキップ）。
   - **週足配信**：**毎週土曜日 09:00〜09:30 JST**。
   - **送信宛先**：各ログインユーザーの登録メールアドレス宛に個別配信。

---

## 4. エッジケース・例外処理・非機能要件

### 4.1 エッジケースとフォールバック処理

| シナリオ | 発生条件 | 処理・フォールバック仕様 |
| :--- | :--- | :--- |
| **株価データ取得失敗** | 上場廃止銘柄・存在しないコード・通信エラー | 配信バッチを停止させず、該当銘柄の位置に「⚠️ 株価データ取得失敗（コード: XXXX）」の注記カードを表示してメール送信を完了。 |
| **登録銘柄 0件** | ユーザーがお気に入りを1件も登録していない | 当該ユーザーへのメール送信処理をスキップ（ログに記録）。 |
| **東証休業日** | 祝日・年末年始など | 配信バッチ起動時に営業日チェックを行い、取引がない場合は「本日は休業日のため配信をスキップします」とログ出力して終了。 |
| **GitHub Actions実行遅延** | Actionsインフラの混雑 | 17:00〜17:30 JSTでの配信完了を許容範囲とする。 |

### 4.2 非機能要件 (NFR)

1. **完全無料運用 (0円)**
   - GitHub Pages (無料ホスティング), Supabase Free Tier (500MB DB/50kユーザー), GitHub Actions (2,000分/月), Gmail SMTP (500通/日) のすべて無料枠内で収める。
2. **パフォーマンス**
   - Python配信スクリプトにおいて、銘柄データ取得およびチャート画像生成を並行処理（`ThreadPoolExecutor`）することで、全処理を1分以内に完了させる。
3. **セキュリティ**
   - パスワードはSupabase Authによる暗号化管理。
   - データベースアクセスはRow Level Security (RLS) でユーザーごとに隔離。
   - APIキーおよびSMTP設定は GitHub Repository Secrets にて保護。

---

## 5. 受け入れ基準 (Acceptance Criteria)

1. [ ] **認証**: 管理者が発行したアカウントでのみログイン・ログアウトができること。
2. [ ] **お気に入り登録**: 4桁コードの銘柄を追加でき、31件目の追加時にエラーが表示され登録がブロックされること。
3. [ ] **日足チャート配信**: 平日17:00のGitHub Actions実行により、50営業日分の日足チャート付きHTMLメールが指定アドレスに届くこと。
4. [ ] **週足チャート配信**: 土曜09:00の実行により、52週分の週足チャート付きHTMLメールが指定アドレスに届くこと。
5. [ ] **モバイル表示**: スマホの標準メールアプリで横スクロールやレイアウト崩れなく全チャートを快適に閲覧できること。
